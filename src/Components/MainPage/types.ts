export interface IProps {
  name: string;
}
export interface iMainContent {
  title: string;
  subTitle: string;
}
export interface iMainContentCard {
  titleImg: string;
  author: string;
  authorIcon: string;
  img: string;
  test: {  authorIcon: string;
    img: string;}
}
export interface iBg {
  bg: string;
}
